<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>원격 지원 안내 - PharmChecker</title>
  <link rel="stylesheet" href="/bulma.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      padding-top: 60px;
    }

    /* 헤더 네비게이션 */
    .header {
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      background: white;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      z-index: 1000;
      padding: 12px 0;
    }

    .header .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 24px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .logo {
      font-size: 24px;
      font-weight: 700;
      color: #191f28;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .nav-menu {
      display: flex;
      align-items: center;
      gap: 32px;
    }

    .nav-item {
      position: relative;
    }

    .nav-link {
      font-size: 15px;
      font-weight: 500;
      color: #191f28;
      text-decoration: none;
      padding: 8px 16px;
      border-radius: 6px;
      transition: background-color 0.2s;
      display: flex;
      align-items: center;
      gap: 6px;
      cursor: pointer;
    }

    .nav-link:hover {
      background-color: #f8f9fa;
    }

    .nav-dropdown-arrow {
      width: 0;
      height: 0;
      border-left: 4px solid transparent;
      border-right: 4px solid transparent;
      border-top: 5px solid #6b7684;
      transition: transform 0.2s;
    }

    .nav-item.active .nav-dropdown-arrow {
      transform: rotate(180deg);
    }

    .nav-dropdown-menu {
      position: absolute;
      top: calc(100% + 8px);
      left: 0;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.15);
      min-width: 220px;
      opacity: 0;
      visibility: hidden;
      transform: translateY(-10px);
      transition: all 0.3s ease;
      z-index: 9999;
    }

    .nav-dropdown-menu.active {
      opacity: 1;
      visibility: visible;
      transform: translateY(0);
    }

    .nav-dropdown-item {
      padding: 12px 20px;
      cursor: pointer;
      transition: background-color 0.2s;
      display: flex;
      align-items: center;
      gap: 10px;
      border-bottom: 1px solid #f0f0f0;
      text-decoration: none;
      color: #191f28;
    }

    .nav-dropdown-item:first-child {
      border-top-left-radius: 12px;
      border-top-right-radius: 12px;
    }

    .nav-dropdown-item:last-child {
      border-bottom-left-radius: 12px;
      border-bottom-right-radius: 12px;
      border-bottom: none;
    }

    .nav-dropdown-item:hover {
      background-color: #f8f9fa;
    }

    .nav-dropdown-item-icon {
      font-size: 16px;
    }

    .nav-dropdown-item-text {
      font-size: 14px;
      font-weight: 500;
    }

    .user-profile {
      position: relative;
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 8px 16px;
      border-radius: 24px;
      cursor: pointer;
      transition: background-color 0.2s;
    }

    .user-info:hover {
      background-color: #f8f9fa;
    }

    .user-avatar {
      width: 36px;
      height: 36px;
      border-radius: 50%;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 600;
      font-size: 14px;
    }

    .user-avatar img {
      width: 100%;
      height: 100%;
      border-radius: 50%;
      object-fit: cover;
    }

    .user-name {
      font-size: 15px;
      font-weight: 500;
      color: #191f28;
    }

    .dropdown-arrow {
      width: 0;
      height: 0;
      border-left: 4px solid transparent;
      border-right: 4px solid transparent;
      border-top: 5px solid #6b7684;
      transition: transform 0.2s;
    }

    .user-info.active .dropdown-arrow {
      transform: rotate(180deg);
    }

    .dropdown-menu {
      position: absolute;
      top: calc(100% + 8px);
      right: 0;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.15);
      min-width: 200px;
      opacity: 0;
      visibility: hidden;
      transform: translateY(-10px);
      transition: all 0.3s ease;
      z-index: 9999;
    }

    .dropdown-menu.active {
      opacity: 1;
      visibility: visible;
      transform: translateY(0);
      display: block;
    }

    .dropdown-item {
      padding: 14px 20px;
      cursor: pointer;
      transition: background-color 0.2s;
      display: flex;
      align-items: center;
      gap: 12px;
      border-bottom: 1px solid #f0f0f0;
    }

    .dropdown-item:first-child {
      border-top-left-radius: 12px;
      border-top-right-radius: 12px;
    }

    .dropdown-item:last-child {
      border-bottom-left-radius: 12px;
      border-bottom-right-radius: 12px;
      border-bottom: none;
    }

    .dropdown-item:hover {
      background-color: #f8f9fa;
    }

    .dropdown-item-icon {
      font-size: 18px;
    }

    .dropdown-item-text {
      font-size: 14px;
      color: #191f28;
      font-weight: 500;
    }

    .dropdown-item.logout {
      color: #e74c3c;
    }

    .dropdown-item.logout:hover {
      background-color: #fee;
    }

    .dropdown-item.logout .dropdown-item-text {
      color: #e74c3c;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 24px;
    }
    .guide-section {
      max-width: 900px;
      margin: 0 auto;
    }
    .info-box {
      background: #f0f7ff;
      border-left: 4px solid #3273dc;
      padding: 1.5rem;
      margin-bottom: 2rem;
      border-radius: 4px;
    }
    .warning-box {
      background: #fff8e6;
      border-left: 4px solid #ffdd57;
      padding: 1.5rem;
      margin-bottom: 2rem;
      border-radius: 4px;
    }
    .step-card {
      background: white;
      border: 1px solid #dbdbdb;
      border-radius: 8px;
      padding: 1.5rem;
      margin-bottom: 1.5rem;
    }
    .step-number {
      display: inline-block;
      width: 40px;
      height: 40px;
      line-height: 40px;
      text-align: center;
      background: #3273dc;
      color: white;
      border-radius: 50%;
      font-weight: 700;
      font-size: 1.2rem;
      margin-right: 1rem;
    }
    .step-title {
      display: flex;
      align-items: center;
      margin-bottom: 1rem;
    }
    .step-content {
      margin-left: 56px;
    }
    .feature-list {
      list-style: none;
      padding: 0;
    }
    .feature-list li {
      padding: 0.5rem 0;
      padding-left: 2rem;
      position: relative;
    }
    .feature-list li:before {
      content: "✓";
      position: absolute;
      left: 0;
      color: #48c774;
      font-weight: 700;
      font-size: 1.2rem;
    }
    .contact-box {
      background: #f9f9f9;
      border: 2px solid #3273dc;
      border-radius: 8px;
      padding: 2rem;
      text-align: center;
      margin: 2rem 0;
    }
    .contact-phone {
      font-size: 2rem;
      font-weight: 700;
      color: #3273dc;
      margin: 1rem 0;
    }
    .button-group {
      position: sticky;
      bottom: 0;
      background: white;
      padding: 1.5rem 0;
      border-top: 1px solid #dbdbdb;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <!-- 헤더 -->
  <header class="header">
    <div class="container">
      <a href="/pharmchecker" class="logo" style="text-decoration: none;">
        <span>TAID SOFT</span>
      </a>
      
      <div class="nav-menu">
        <!-- 고객센터 메뉴 -->
        <div class="nav-item" id="customerCenterNav">
          <div class="nav-link" onclick="toggleNavDropdown()">
            <span>고객센터</span>
            <div class="nav-dropdown-arrow"></div>
          </div>
          <div class="nav-dropdown-menu" id="navDropdownMenu">
            <a href="/support/tickets" class="nav-dropdown-item">
              <span class="nav-dropdown-item-icon">✍️</span>
              <span class="nav-dropdown-item-text">문의하기</span>
            </a>
            <a href="/support/faq" class="nav-dropdown-item">
              <span class="nav-dropdown-item-icon">❓</span>
              <span class="nav-dropdown-item-text">FAQ</span>
            </a>
          </div>
        </div>

        <!-- 사용자 프로필 -->
        <div class="user-profile">
          <div class="user-info" onclick="toggleDropdown()">
            <div class="user-avatar" id="userAvatar">
              <span id="userInitial">U</span>
            </div>
            <span class="user-name" id="userName">게스트</span>
            <div class="dropdown-arrow"></div>
          </div>
          <div class="dropdown-menu" id="dropdownMenu">
            <div class="dropdown-item" onclick="window.location.href='/my-subscription'">
              <span class="dropdown-item-icon">📋</span>
              <span class="dropdown-item-text">내 구독 정보</span>
            </div>
            <div class="dropdown-item" onclick="window.location.href='/payment-history'">
              <span class="dropdown-item-icon">💳</span>
              <span class="dropdown-item-text">결제 내역</span>
            </div>
            <div class="dropdown-item" onclick="viewProfile()">
              <span class="dropdown-item-icon">👤</span>
              <span class="dropdown-item-text">프로필 보기</span>
            </div>
            <div class="dropdown-item logout" onclick="logout()">
              <span class="dropdown-item-icon">🚪</span>
              <span class="dropdown-item-text">로그아웃</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </header>

  <section class="section">
    <div class="container guide-section">
      <h1 class="title">원격 지원 안내</h1>
      <p class="subtitle">PharmChecker 원격 지원 서비스를 이용하시려면 아래 절차를 따라주세요.</p>

      <!-- 고객센터 안내 -->
      <div class="contact-box">
        <p style="font-size: 1.1rem; margin-bottom: 0.5rem;">원격 지원 고객센터</p>
        <div class="contact-phone">📞 1588-8989</div>
        <p style="color: #7a7a7a;">평일 09:00 - 18:00 (주말 및 공휴일 제외)</p>
      </div>

      <!-- 중요 안내 -->
      <div class="warning-box">
        <h3 class="title is-5">⚠️ 중요 안내사항</h3>
        <ul style="margin-left: 1.5rem;">
          <li><strong>고객센터(1588-8989)로 먼저 전화</strong>주셔서 원격 지원이 가능합니다.</li>
          <li>전화 연결 후 아래 절차에 따라 <strong>원격 지원을 진행</strong>합니다.</li>
          <li>TeamViewer QuickSupport는 <strong>설치가 필요 없는</strong> 일회용 프로그램입니다.</li>
          <li>원격 지원 중 <strong>개인정보 및 결제정보</strong>가 노출되지 않도록 주의해주세요.</li>
        </ul>
      </div>

      <!-- 원격 지원 절차 -->
      <h2 class="title is-4" style="margin-top: 2rem; margin-bottom: 1.5rem;">원격 지원 절차</h2>

      <!-- 1단계: 고객센터 연결 확인 -->
      <div class="step-card" style="border: 2px solid #3273dc; background: #f0f7ff;">
        <div class="step-title">
          <span class="step-number">1</span>
          <h3 class="title is-5" style="margin: 0;">고객센터 연결 확인</h3>
        </div>
        <div class="step-content">
          <p><strong style="color: #3273dc;">1588-8989</strong>로 전화하셔서 상담원과 연결되셨나요?</p>
          <ul style="margin-left: 1.5rem; margin-top: 0.5rem;">
            <li>상담원과 통화가 연결된 후 아래 "확인" 버튼을 눌러주세요</li>
            <li>원격 지원이 자동으로 접수됩니다</li>
          </ul>
          <p style="margin-top: 1rem;">
            <button id="btnConfirmConnection" class="button is-primary is-large" onclick="confirmConnection()">
              <span>✓ 고객센터와 연결되었습니다</span>
            </button>
          </p>
        </div>
      </div>

      <!-- 2단계: TeamViewer 다운로드 -->
      <div class="step-card">
        <div class="step-title">
          <span class="step-number">2</span>
          <h3 class="title is-5" style="margin: 0;">TeamViewer QuickSupport 다운로드 및 설치</h3>
        </div>
        <div class="step-content">
          <p>설치가 필요 없는 일회용 원격 지원 프로그램을 다운로드합니다.</p>
          <p style="margin-top: 1rem;">
            <a href="/support/remote/download" class="button is-info">
              <span>다운로드 페이지로 이동</span>
            </a>
          </p>
        </div>
      </div>

      <!-- 3단계: TeamViewer 실행 및 ID 전달 -->
      <div class="step-card">
        <div class="step-title">
          <span class="step-number">3</span>
          <h3 class="title is-5" style="margin: 0;">TeamViewer 실행 및 ID 전달</h3>
        </div>
        <div class="step-content">
          <p>TeamViewer QuickSupport를 실행하고 화면에 표시되는 <strong>ID</strong>를 상담원에게 알려주세요.</p>
          <ul style="margin-left: 1.5rem; margin-top: 0.5rem;">
            <li>9자리 숫자 ID가 표시됩니다 (예: 123 456 789)</li>
            <li>상담원이 연결 요청을 보냅니다</li>
            <li>"연결 허용" 버튼을 클릭하세요</li>
          </ul>
        </div>
      </div>

      <!-- 4단계: 원격 지원 진행 -->
      <div class="step-card">
        <div class="step-title">
          <span class="step-number">4</span>
          <h3 class="title is-5" style="margin: 0;">원격 지원 진행</h3>
        </div>
        <div class="step-content">
          <p>상담원이 화면을 보면서 문제를 해결해드립니다.</p>
          <ul style="margin-left: 1.5rem; margin-top: 0.5rem;">
            <li>필요 시 상담원이 화면을 제어할 수 있습니다</li>
            <li>언제든지 연결을 종료할 수 있습니다</li>
            <li>개인정보가 보이는 화면은 미리 닫아주세요</li>
          </ul>
        </div>
      </div>

      <!-- TeamViewer 장점 -->
      <div class="info-box" style="margin-top: 2rem;">
        <h3 class="title is-5">✨ TeamViewer QuickSupport 특징</h3>
        <ul class="feature-list">
          <li><strong>설치 불필요</strong> - 다운로드 후 바로 실행 가능</li>
          <li><strong>일회용</strong> - 사용 후 자동으로 연결 종료</li>
          <li><strong>보안</strong> - ID는 매번 새로 생성되며 저장되지 않음</li>
          <li><strong>무료</strong> - 고객은 별도 비용 없이 사용 가능</li>
          <li><strong>전 세계 1위</strong> - 가장 많이 사용되는 원격 지원 솔루션</li>
        </ul>
      </div>

      <!-- TeamViewer 장점은 그대로 유지 -->
    </div>
  </section>

  <script>
    // 페이지 로드 시 연결 상태 확인
    window.addEventListener('DOMContentLoaded', function() {
      const userId = localStorage.getItem('userId');
      const sessionNumber = sessionStorage.getItem('remote_session_' + userId);
      
      if (sessionNumber) {
        // 이미 연결된 상태
        updateButtonState(true, sessionNumber);
      }
    });

    // 버튼 상태 업데이트
    function updateButtonState(isConnected, sessionNumber) {
      const btn = document.getElementById('btnConfirmConnection');
      if (isConnected) {
        btn.innerHTML = '<span>✅ 연결 완료 (세션: ' + sessionNumber + ')</span>';
        btn.disabled = true;
        btn.classList.remove('is-primary');
        btn.classList.add('is-success');
        
        // 2단계 강조
        const step2 = document.querySelectorAll('.step-card')[1];
        if (step2) {
          step2.style.border = '2px solid #3273dc';
          step2.style.background = '#f0f7ff';
        }
      }
    }

    // 고객센터 연결 확인
    async function confirmConnection() {
      const userId = localStorage.getItem('userId');
      if (!userId) {
        alert('로그인이 필요합니다.');
        window.location.href = '/login';
        return;
      }

      // 이미 연결된 상태인지 확인
      const existingSession = sessionStorage.getItem('remote_session_' + userId);
      if (existingSession) {
        alert('이미 원격 지원이 접수되었습니다.\n세션 번호: ' + existingSession);
        return;
      }

      if (!confirm('고객센터 상담원과 통화가 연결되셨습니까?\n\n"확인"을 누르면 원격 지원이 접수됩니다.')) {
        return;
      }

      const btn = document.getElementById('btnConfirmConnection');
      btn.disabled = true;
      btn.classList.add('is-loading');

      try {
        const response = await fetch('/support/api/remote/simple', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-User-Id': userId
          }
        });

        const result = await response.json();

        if (result.success) {
          // 세션 번호 저장
          sessionStorage.setItem('remote_session_' + userId, result.session_number);
          
          // 버튼 상태 업데이트
          btn.classList.remove('is-loading');
          updateButtonState(true, result.session_number);
          
          alert(`✅ 원격 지원이 접수되었습니다.\n\n세션 번호: ${result.session_number}\n\n이제 아래 2단계로 진행해주세요.`);
          
          // 2단계로 스크롤 이동
          const step2 = document.querySelectorAll('.step-card')[1];
          if (step2) {
            step2.scrollIntoView({ behavior: 'smooth', block: 'center' });
          }
        } else {
          alert('❌ ' + (result.error || '원격 지원 접수에 실패했습니다.'));
          btn.disabled = false;
          btn.classList.remove('is-loading');
        }
      } catch (error) {
        console.error('Error:', error);
        alert('오류가 발생했습니다. 다시 시도해주세요.');
        btn.disabled = false;
        btn.classList.remove('is-loading');
      }
    }

    // URL에 type 파라미터가 있으면 자동으로 문의 작성 페이지로 이동 시 선택
    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.get('direct') === 'true') {
      window.location.href = '/support/new?type=remote_request';
    }

    // 헤더 공통 JavaScript
    function toggleDropdown() {
      const dropdown = document.getElementById('dropdownMenu');
      const userInfo = document.querySelector('.user-info');
      if (dropdown && userInfo) {
        dropdown.classList.toggle('active');
        userInfo.classList.toggle('active');
        const navDropdown = document.getElementById('navDropdownMenu');
        const navItem = document.getElementById('customerCenterNav');
        if (navDropdown && navItem) {
          navDropdown.classList.remove('active');
          navItem.classList.remove('active');
        }
      }
    }
    
    function toggleNavDropdown() {
      const navDropdown = document.getElementById('navDropdownMenu');
      const navItem = document.getElementById('customerCenterNav');
      if (navDropdown && navItem) {
        navDropdown.classList.toggle('active');
        navItem.classList.toggle('active');
      }
      const dropdown = document.getElementById('dropdownMenu');
      const userInfo = document.querySelector('.user-info');
      if (dropdown && userInfo) {
        dropdown.classList.remove('active');
        userInfo.classList.remove('active');
      }
    }
    
    document.addEventListener('click', function(event) {
      const userProfile = document.querySelector('.user-profile');
      const dropdown = document.getElementById('dropdownMenu');
      const userInfo = document.querySelector('.user-info');
      const customerCenterNav = document.getElementById('customerCenterNav');
      const navDropdown = document.getElementById('navDropdownMenu');
      
      if (userProfile && !userProfile.contains(event.target)) {
        if (dropdown) dropdown.classList.remove('active');
        if (userInfo) userInfo.classList.remove('active');
      }
      
      if (customerCenterNav && !customerCenterNav.contains(event.target)) {
        if (navDropdown) navDropdown.classList.remove('active');
        if (customerCenterNav) customerCenterNav.classList.remove('active');
      }
    });

    function viewProfile() {
      const userName = localStorage.getItem('name');
      const userEmail = localStorage.getItem('email');
      const pharmacyName = localStorage.getItem('pharmacy_name');
      if (userName) {
        let profileInfo = `프로필 정보\n\n이름: ${userName}\n이메일: ${userEmail}`;
        if (pharmacyName) profileInfo += `\n약국명: ${pharmacyName}`;
        alert(profileInfo);
      }
    }
    
    function logout() {
      if (confirm('로그아웃 하시겠습니까?')) {
        localStorage.clear();
        window.location.href = '/login';
      }
    }

    window.addEventListener('DOMContentLoaded', function() {
      const userName = localStorage.getItem('name');
      const userPicture = localStorage.getItem('picture');
      const userNameEl = document.getElementById('userName');
      const userAvatarEl = document.getElementById('userAvatar');
      const userInitialEl = document.getElementById('userInitial');
      
      if (userNameEl && userName) {
        userNameEl.textContent = userName + '님';
        if (userPicture && userAvatarEl) {
          userAvatarEl.innerHTML = `<img src="${userPicture}" alt="프로필">`;
        } else if (userInitialEl) {
          const initial = (userName || 'G').charAt(0).toUpperCase();
          userInitialEl.textContent = initial;
        }
      }
    });
  </script>
</body>
</html>
